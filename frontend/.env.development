# This file tells create-react-app to look for environment variables in the parent directory
# The React app will use the REACT_APP_* variables from the root .env file
# This allows us to maintain a single source of truth for environment variables 