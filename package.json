{
  "name": "pladder-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Purple Ladder AI Platform Monorepo",
  "scripts": {
    "test": "echo \"Run tests with: docker compose exec backend pytest && docker compose exec frontend npm test\" && exit 0"
  },
  "workspaces": [
    "frontend"
  ]
}
